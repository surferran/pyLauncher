pyInstall instructions:

as in : file:///C:/Users/Ran_the_User/Documents/RAN/NOW/Python/PyInstaller%20Manual.html#what-pyinstaller-does-and-how-it-does-it
open command prompt in main .py path. run the next line:
pyinstaller --onefile --windowed myLauncher.py

it will create build and dist directories.
need to note to manually add extra files such as user files and images.
find the .exe resultant file in the 'dist' path. copy it to the main path where the main .py script file.

